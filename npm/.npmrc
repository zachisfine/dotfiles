# ~/.npmrc — npm defaults
# Part of ~/dotfiles — managed via install.sh

# Lock exact versions (no ^ or ~ prefix)
save-exact=true

# Suppress funding messages
fund=false

# Only warn on audit (don't block installs)
audit-level=moderate

# Don't open browser for npm docs / npm repo
browser=false
