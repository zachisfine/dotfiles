# ~/.config/pip/pip.conf — pip defaults
# Part of ~/dotfiles — managed via install.sh

[global]
# Require an active virtualenv for pip install
require-virtualenv = true

[install]
# Compile bytecode during install
compile = true
